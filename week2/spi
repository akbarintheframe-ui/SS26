//master code
#include <SPI.h>
void setup() {
 pinMode(10, OUTPUT);
 SPI.begin();
 digitalWrite(10, HIGH);
}
void loop() {
 digitalWrite(10, LOW);
 SPI.transfer(8);
 digitalWrite(10, HIGH);
 delay(1000);

 //Slave code
#include <SPI.h>
volatile byte receivedData;
void setup() {
 Serial.begin(9600);
 pinMode(12, OUTPUT);


 SPCR |= _BV(SPE);

 SPI.attachInterrupt();
}
ISR (SPI_STC_vect) {
 receivedData = SPDR;
 Serial.println(receivedData);
}
void loop() {
}
